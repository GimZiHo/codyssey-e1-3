# Mini NPU Simulator 설계서

## 1. 설계 목표

이 프로젝트는 `python main.py` 한 번으로 실행되는 Python 콘솔 애플리케이션이다. 필수 과제의 두 실행 모드를 우선 완성하고, 같은 핵심 함수를 재사용해 보너스 기능인 패턴 생성기와 1차원 배열 기반 MAC 최적화를 제공한다.

설계 원칙은 다음과 같다.

- Python 3.8 이상과 표준 라이브러리만 사용한다.
- MAC 연산은 중첩 반복문으로 직접 구현한다.
- 사용자 입력, JSON 데이터, 계산, 판정, 출력 책임을 분리한다.
- 잘못된 사용자 입력은 재입력시키고, 잘못된 JSON 케이스는 해당 케이스만 FAIL 처리한다.
- 모든 판정은 하나의 epsilon 정책과 표준 라벨을 사용한다.
- 실제 성능값과 평가항목 답변은 구현 및 검증 후 마지막에 README에 작성한다.

### 설계를 여러 책임으로 나누는 이유

이 과제는 MAC 점수뿐 아니라 입력 검증, JSON 해석, 부동소수점 비교, 오류 복구, 성능 측정을 함께 평가한다. 모든 코드를 `main.py`에 길게 작성하면 기능 사이의 관계를 설명하기 어렵고 한 부분을 수정했을 때 다른 부분이 깨졌는지 확인하기도 어렵다.

- 계산과 입출력을 분리하면 MAC 알고리즘만 독립적으로 이해하고 테스트할 수 있다.
- 검증과 계산을 분리하면 잘못된 데이터가 애매한 인덱스 오류가 되기 전에 정확한 원인을 알릴 수 있다.
- 라벨 정규화를 분리하면 외부 표현이 달라도 내부 판정은 `Cross`, `X` 두 의미만 다루게 된다.
- 성능 측정을 분리하면 입력이나 출력 시간이 아니라 N²번 반복하는 계산 비용에 집중할 수 있다.
- 필수 2D 구현을 기준으로 유지하면 보너스 1D 최적화가 더 빠른지만이 아니라 같은 답을 보존하는지도 검증할 수 있다.
- README를 마지막에 작성하면 예상이 아니라 실제 테스트와 측정 결과를 근거로 설계를 설명할 수 있다.

## 2. 디렉터리 구조

```text
codyssey-e1-3/
├── main.py                    # 실행 진입점: 메뉴와 모드 라우팅
├── data.json                  # 과제 제공 필터 및 패턴 데이터
├── mini_npu/
│   ├── __init__.py
│   ├── constants.py           # EPSILON, 반복 횟수, 표준 라벨
│   ├── core.py                # 2차원/1차원 MAC, 점수 비교 및 판정
│   ├── validation.py          # 정사각 행렬, 크기, 숫자 검증
│   ├── labels.py              # normalize_label
│   ├── loader.py              # data.json 로드, 키 파싱, 스키마 검증
│   ├── generator.py           # Cross/X 패턴 자동 생성(보너스)
│   ├── benchmark.py           # 반복 측정 및 2D/1D 성능 비교
│   ├── models.py              # 분석 결과/실패 정보 데이터 클래스
│   ├── modes.py               # 모드 1, 모드 2, 보너스 모드 실행 흐름
│   └── presentation.py        # 콘솔 표와 결과 요약 출력
├── tests/
│   ├── __init__.py
│   ├── test_core.py
│   ├── test_validation.py
│   ├── test_labels.py
│   ├── test_loader.py
│   ├── test_generator.py
│   ├── test_benchmark.py
│   └── fixtures/
│       ├── valid_data.json
│       └── invalid_data.json
├── README.md                  # 평가자가 보는 최종 실행/설계/결과 보고서
├── LEARNING_LOG.md            # 단계별 입력·처리·출력, 테스트, 핵심 개념 기록
├── DESIGN.md                  # 이 설계서
├── problem.md                 # 원문 기반 과제 요구사항
└── evaluation.md              # 원문 기반 평가 체크리스트
```

`main.py`에는 복잡한 로직을 두지 않는다. 따라서 평가자는 `python main.py`로 바로 실행할 수 있고, 각 평가항목에 대응하는 로직은 이름이 명확한 모듈과 함수에서 확인할 수 있다.

모듈을 여러 파일로 나누더라도 실행 애플리케이션은 하나다. 사용자는 항상 `python main.py`로 시작하며, 모듈 분리의 목적은 제출물을 복잡하게 만드는 것이 아니라 각 기능의 책임과 설계 이유를 읽기 쉽게 만드는 것이다.

## 3. 주요 데이터와 상수

### 행렬 표현

- 기본 표현: `typing.List[typing.List[float]]`
- 보너스용 평탄화 표현: `typing.List[float]`
- 별도 외부 자료형이나 NumPy 배열은 사용하지 않는다.

### 상수

```python
EPSILON = 1e-9
BENCHMARK_REPEATS = 10
CROSS = "Cross"
X_LABEL = "X"
UNDECIDED = "UNDECIDED"
```

반복 횟수는 최소 10회를 보장한다. 벤치마크 함수는 테스트 편의를 위해 반복 횟수를 인자로 받을 수 있지만, 실제 콘솔 실행의 기본값은 10이다.

### 분석 결과 모델

`models.py`에서 `dataclass`를 사용해 다음 정보를 구조화한다.

- `CaseResult`: 케이스 ID, 크기, 두 점수, 판정, expected, PASS/FAIL, 실패 분류와 사유
- `BenchmarkResult`: 크기, 반복 횟수, 평균 시간(ms), 연산 횟수
- `OptimizationResult`: 같은 입력의 2D/1D 평균 시간과 결과 일치 여부

`CaseResult`의 크기, 점수, 판정은 선택값(`Optional`)으로 둔다. 패턴 키나 행렬 구조가 잘못되어 MAC을 실행하지 못한 경우에는 점수가 존재하지 않기 때문이다. 이때 점수를 `0.0`으로 저장하면 실제 계산 결과가 0점인 경우와 데이터 오류를 구별할 수 없다.

이 구조를 사용하면 계산 코드가 문자열 출력 형식에 의존하지 않고 테스트할 수 있다.

### 실제 `data.json` 분석 결과

설계를 구현하기 전에 실제 데이터 파일을 읽어 구조와 기준 결과를 확인했다. 먼저 데이터를 분석하는 이유는 문서에 적힌 예상 스키마만 믿고 구현했다가 실제 키나 중첩 구조가 달라지는 문제를 막기 위해서다.

- 최상위 키는 `meta`, `filters`, `patterns`이다.
- `meta`는 `version: "1.0"`, `type: "json"`이다.
- 필터 그룹은 `size_5`, `size_13`, `size_25`이다.
- 각 필터 그룹의 키는 `cross`, `x`이다.
- 패턴은 `size_5_1`부터 `size_25_2`까지 총 6개다.
- 모든 필터와 패턴 행렬은 키에 표시된 N과 실제 행·열 크기가 일치한다.
- expected에는 `+`, `x`가 사용되므로 `Cross`, `X`로 정규화해야 한다.
- 값에는 `0.1`, `0.3`, `0.9`, `4.9`, `7.5` 같은 실수가 포함되어 epsilon 비교가 실제로 필요하다.

epsilon `1e-9`를 적용한 기준 결과는 다음과 같다.

| 케이스 | expected | Cross 점수 | X 점수 | 판정 | 결과 |
|---|---|---:|---:|---|---|
| `size_5_1` | X | 0.9 | 0.9 | UNDECIDED | FAIL |
| `size_5_2` | Cross | 8.9 | 0.1 | Cross | PASS |
| `size_13_1` | X | 0.3 | 14.7 | X | PASS |
| `size_13_2` | Cross | 7.5 | 7.5 | UNDECIDED | FAIL |
| `size_25_1` | X | 4.9 | 4.9 | UNDECIDED | FAIL |
| `size_25_2` | Cross | 52.9 | 0.1 | Cross | PASS |

예상 집계는 **총 6개 / PASS 3개 / FAIL 3개**다. 세 FAIL은 프로그램 오류가 아니라 두 점수가 epsilon 범위에서 같은 경우 `UNDECIDED`로 판정하라는 요구사항을 적용한 결과다. 따라서 README에서는 FAIL을 숨기거나 억지로 PASS로 바꾸지 않고, 수치 비교 정책에 따른 정상 결과로 분석한다.

## 4. 핵심 함수 설계

### 4.1 MAC 연산

```python
def mac_2d(pattern: List[List[float]],
           filter_matrix: List[List[float]]) -> float:
    total = 0.0
    for row in range(len(pattern)):
        for col in range(len(pattern[row])):
            total += pattern[row][col] * filter_matrix[row][col]
    return total
```

호출 전에 두 행렬이 같은 크기의 정사각 행렬인지 검증한다. 계산 함수 내부에서도 예상하지 못한 직접 호출을 방어할 수 있도록 크기 오류에는 의미 있는 `ValueError`를 발생시킨다.

점수가 높다는 것은 같은 위치에서 큰 값끼리 곱해져 누적된 값이 더 크다는 뜻이다. 이 과제의 동일한 크기·동일한 척도 필터끼리 비교하는 조건에서 더 높은 내적 점수를 더 유사한 패턴으로 판정한다.

### 4.2 epsilon 비교와 판정

```python
def compare_scores(score_a, score_b, epsilon=EPSILON):
    if abs(score_a - score_b) < epsilon:
        return 0
    return 1 if score_a > score_b else -1
```

- 모드 1은 비교 결과 `0`을 사용자 친화적인 `판정 불가`로 표시한다.
- 모드 2는 같은 비교 결과 `0`을 시스템 표준값 `UNDECIDED`로 표시하고 expected와 일치할 수 없는 FAIL로 집계한다.
- 비교 수학은 같고 표현 및 집계 정책만 모드 요구사항에 맞게 다르다.

### 4.3 라벨 정규화

```python
LABEL_ALIASES = {
    "+": "Cross",
    "cross": "Cross",
    "x": "X",
}
```

`normalize_label(value)`는 문자열의 앞뒤 공백과 대소문자를 정리한 뒤 표준 라벨을 반환한다. 알 수 없는 라벨은 조용히 통과시키지 않고 명시적인 검증 오류로 처리한다.

별도 함수로 분리하면 JSON filter 키와 expected 값이 같은 정책을 공유하고, 새 라벨 `o`가 생겨도 매핑과 테스트를 한곳에서 확장할 수 있다.

### 4.4 패턴 키 해석

`parse_pattern_key(key)`는 정규식 `^size_(\d+)_(.+)$`로 N과 케이스 인덱스를 추출한다.

예를 들어 `size_13_2`는 N=13이며 `filters["size_13"]`과 매칭된다. 형식이 틀리거나 대응 필터가 없으면 해당 패턴만 FAIL 처리한다.

## 5. 검증 및 오류 정책

### 사용자 입력

`read_square_matrix(name, size=3)`가 정확히 3개 행을 순서대로 입력받는다.

- 한 행의 열이 3개가 아니면 해당 행부터 다시 입력한다.
- 숫자로 변환되지 않으면 오류 문구를 출력하고 해당 행부터 다시 입력한다.
- 빈 문자열이나 `NaN`, `Infinity`처럼 유한하지 않은 값은 거부한다.
- 필터 A, 필터 B, 패턴에 같은 입력 함수를 재사용한다.

### JSON 전체 파일 오류

다음 오류는 모드 2를 정상 종료시키되 분석을 시작할 수 없음을 분명히 출력한다.

- 파일 없음
- JSON 문법 오류
- 최상위 값이 객체가 아님
- `filters` 또는 `patterns` 섹션 없음

`meta`는 계산 자체에 필수인 값은 아니므로 누락되거나 예상값과 다를 때 경고를 출력하고 분석은 계속한다. 확인할 값은 `version == "1.0"`, `type == "json"`이다. 이 정책은 데이터 버전 변화는 사용자에게 알리면서도 정상적인 `filters`와 `patterns`까지 불필요하게 버리지 않기 위한 것이다. 반면 두 필수 섹션이 없으면 계산할 수 없으므로 모드 2를 종료한다.

### JSON 케이스 오류

다음 오류는 전체 프로그램을 중단하지 않고 `CaseResult(passed=False)`로 기록한다.

- 패턴 키 형식 오류
- 대응하는 `size_N` 필터 없음
- Cross/X 필터 누락 또는 알 수 없는 필터 라벨
- `input` 또는 `expected` 누락
- 숫자가 아닌 행렬 값
- 비정사각 행렬 또는 선언된 N과 다른 크기
- 필터와 패턴 크기 불일치
- 알 수 없는 expected 라벨

실패 사유는 `데이터/스키마`, `로직`, `수치 비교` 가운데 원인을 식별할 수 있는 문구로 남긴다. 이 분류의 목적은 모든 FAIL을 코드 버그로 오해하지 않게 하는 것이다. 실제 데이터의 `size_5_1`, `size_13_2`, `size_25_1`은 `수치 비교: epsilon 범위 내 동점`으로 기록한다.

## 6. 실행 흐름

### 메인 메뉴

```text
1. 사용자 입력 (3x3)
2. data.json 분석
3. 패턴 생성기 / 최적화 비교 (보너스)
0. 종료
```

잘못된 메뉴 값은 안내 후 다시 선택하도록 한다. 필수 모드 번호 1과 2는 과제 예시와 동일하게 유지한다.

### 모드 1: 사용자 입력

```text
메뉴 선택
  → 필터 A 입력/검증
  → 필터 B 입력/검증
  → 저장된 필터 표시 또는 저장 확인
  → 패턴 입력/검증
  → A/B MAC 점수 계산
  → epsilon 비교
  → A/B/판정 불가 출력
  → 같은 3×3 입력으로 10회 평균 성능 측정
```

성능 측정의 타이머 경계에는 `mac_2d()` 호출만 포함한다. `input`, `print`, 파일 읽기, 검증은 포함하지 않는다.

### 모드 2: JSON 분석

```text
data.json 로드
  → meta 경고 확인 및 filters/patterns 필수 스키마 확인
  → 필터 라벨 정규화
  → 패턴 케이스 순회
      → 키에서 N 추출
      → filters[size_N] 선택
      → 패턴/필터/expected 검증
      → Cross와 X 점수 계산
      → epsilon 판정
      → expected 비교 및 PASS/FAIL 기록
      → 예외는 케이스 FAIL로 변환
  → 케이스별 결과 출력
  → 3/5/13/25 크기 성능 분석
  → 총 테스트/통과/실패와 실패 목록 출력
```

성능 측정 입력은 실행할 때마다 같은 데이터를 선택하도록 다음과 같이 고정한다.

| 크기 | 패턴 | 필터 | 선택 이유 |
|---|---|---|---|
| 3×3 | 생성기의 Cross | 생성기의 Cross | JSON에 3×3 데이터가 없으므로 생성기를 재사용한다. |
| 5×5 | `size_5_1.input` | `size_5.cross` | 해당 크기의 첫 번째 실제 패턴을 사용한다. |
| 13×13 | `size_13_1.input` | `size_13.cross` | 해당 크기의 첫 번째 실제 패턴을 사용한다. |
| 25×25 | `size_25_1.input` | `size_25.cross` | 해당 크기의 첫 번째 실제 패턴을 사용한다. |

고정 입력을 쓰는 목적은 실행마다 다른 데이터가 선택되어 성능 비교가 흔들리는 일을 막는 것이다. 5/13/25 입력이 검증에 실패하면 생성 데이터로 조용히 대체하지 않고 해당 크기를 `SKIP`하며 사유를 출력한다. 데이터 오류를 정상 결과처럼 숨기지 않기 위한 정책이다. 현재 제공 파일은 모든 크기가 유효하므로 정상 실행에서는 SKIP이 발생하지 않는다.

### 모드 3: 보너스

사용자가 양의 홀수 N을 입력하면 N×N Cross와 X를 생성해 출력한다. 지나치게 큰 콘솔 출력을 방지하기 위해 큰 N은 전체 행렬 대신 크기와 일부 미리보기만 표시한다.

같은 생성 패턴으로 다음을 측정한다.

- 기본 `mac_2d`: 2차원 중첩 반복문
- 최적화 `flatten_matrix` + `mac_1d`: 1차원 순차 접근
- 두 구현의 점수 일치 여부
- 각 10회 평균 시간 및 단순 비율

평탄화 비용은 MAC 측정 구간에서 제외한다. 이는 매 MAC 호출 때 변환하는 구현이 아니라, NPU 입력 준비 단계에서 한 번 평탄화한 뒤 여러 번 계산하는 사용 모델을 비교하기 위함이다. 별도로 전체 비용을 설명해 과도한 성능 주장을 피한다.

## 7. 보너스 상세 설계

### Cross 생성

홀수 N의 가운데 인덱스 `center = N // 2`를 구한다. `row == center` 또는 `col == center`인 위치는 1.0, 나머지는 0.0으로 생성한다.

### X 생성

`row == col` 또는 `row + col == N - 1`인 위치는 1.0, 나머지는 0.0으로 생성한다.

짝수 N도 수학적으로 생성할 수 있지만 중심선이 하나로 정해지지 않으므로 사용자용 생성기는 과제 예시와 일관되게 양의 홀수만 허용한다. 내부 함수의 정책과 오류를 테스트한다.

### 1차원 최적화

```python
def flatten_matrix(matrix):
    return [value for row in matrix for value in row]

def mac_1d(pattern, filter_values):
    total = 0.0
    for index in range(len(pattern)):
        total += pattern[index] * filter_values[index]
    return total
```

두 구현 모두 곱셈/누적 횟수는 N²이므로 시간 복잡도는 O(N²)이다. 1차원 버전은 인덱싱과 메모리 접근 형태를 단순화하는 상수 계수 최적화이지 복잡도를 줄이는 최적화는 아니다.

## 8. 성능 측정 설계

- 고해상도 단조 시계인 `time.perf_counter_ns()`를 사용한다.
- 준비, 검증, 변환, 출력은 타이머 밖에서 수행한다.
- 각 크기와 구현을 최소 10회 실행한다.
- 총 경과 나노초를 반복 횟수로 나누고 ms로 변환한다.
- 표에는 `N×N`, 평균 ms, N², 반복 횟수를 표시한다.
- 매우 짧은 3×3 실행은 타이머 해상도와 인터프리터 잡음의 영향을 받는다는 점을 README에 밝힌다.
- 성능 수치는 실행 환경에 의존하므로 저장된 예시값이 아니라 최종 실행에서 실제 측정한 값을 README에 기록한다.

## 9. 테스트 전략

표준 라이브러리 `unittest`만 사용하고 다음 명령으로 전체 테스트를 실행한다.

```bash
python -m unittest discover -s tests -v
```

### 단위 테스트

- 정확한 Cross/Cross, Cross/X MAC 점수(5와 1)
- float 입력의 MAC 결과
- epsilon 바로 안/밖의 동점 경계
- `+`, `cross`, `x`, 대소문자 및 공백 라벨 정규화
- 알 수 없는 라벨 거부
- `size_5_1`, `size_13_2`, `size_25_any` 키 파싱
- 잘못된 패턴 키 거부
- 정사각 행렬, 크기, 숫자, 유한성 검증
- Cross/X 생성기의 3×3, 5×5 예상 배열
- 2D와 1D MAC 점수 일치
- 벤치마크 결과의 반복 횟수, N², 음수가 아닌 시간

### 통합 테스트

- 실제 `data.json`에서 총 6개, PASS 3개, FAIL 3개가 집계되는지 확인
- `size_5_1`, `size_13_2`, `size_25_1`이 UNDECIDED 및 FAIL인지 확인
- `size_5_2`, `size_13_1`, `size_25_2`가 expected와 일치하여 PASS인지 확인
- `meta` 정상, 누락, 버전 불일치에 대한 경고 정책 확인
- 키 오류, 필터 누락, 크기 불일치, 잘못된 expected가 서로 독립된 FAIL로 기록되는지 확인
- 한 케이스 오류 후 다음 정상 케이스가 계속 실행되는지 확인
- `unittest.mock.patch`로 모드 1의 입력 오류 후 재입력 흐름 확인
- 모드 1 동점의 `판정 불가` 출력 확인
- 모드 2 동점의 `UNDECIDED` 및 FAIL 집계 확인
- 성능 표와 전체 결과 요약의 필수 열/문구 확인

### 수동 인수 테스트

- 과제 예시의 3×3 입력을 그대로 실행한다.
- 숫자 대신 문자, 열 부족, 열 초과를 각각 입력한다.
- 실제 `data.json`으로 실행하여 전체 합계를 기록한다.
- 보너스 모드에서 N=3, N=25, 잘못된 짝수/음수 값을 확인한다.
- Python 3.8 문법 호환성을 점검한다.

## 10. 구현 순서

각 번호의 기능을 더 작은 단위로 나누어 한 번에 하나만 구현한다. 기능 하나의 완료 순서는 `구현 → 해당 기능 테스트 → LEARNING_LOG 기록 → 커밋 → main push`로 고정한다. 다음 기능은 앞 기능의 작업 트리가 깨끗하고 원격 저장소에 반영된 뒤 시작한다. 이 규칙의 목적은 각 커밋에서 무엇을 배웠고 어떤 테스트로 검증했는지 명확하게 추적하는 것이다.

### 1단계: 프로젝트 골격과 핵심 계산

- 디렉터리와 패키지 생성
- 상수, 행렬 검증, `mac_2d`, epsilon 비교 구현
- 핵심 단위 테스트 작성

완료 기준: 예시 점수 5/1과 epsilon 경계 테스트가 통과한다.

각 구현 단계가 끝날 때 실행한 테스트, 대표 입력, 내부 처리 과정, 실제 출력, 새로 배운 개념을 `LEARNING_LOG.md`에 기록한다. 자동으로 재실행할 가치가 있는 검증은 설명만 남기지 않고 `tests/`의 `unittest` 코드로 작성한다.

### 2단계: 모드 1

- 행 단위 입력 및 재입력 구현
- A/B 판정과 콘솔 출력 구현
- 3×3 10회 평균 측정 연결
- 입력/출력 통합 테스트 작성

완료 기준: 정상, 파싱 오류, 크기 오류, 동점 시나리오가 모두 통과한다.

### 3단계: 모드 2

- JSON 로더와 전체 스키마 검증 구현
- 라벨 정규화와 패턴 키 파싱 구현
- 케이스 단위 오류 격리, PASS/FAIL, 실패 목록 구현
- fixture 기반 통합 테스트 작성

완료 기준: 한 케이스의 오류가 나머지 분석을 중단하지 않고 합계가 정확하다.

실제 데이터 완료 기준은 총 6개, PASS 3개, FAIL 3개이며 세 FAIL의 사유가 모두 epsilon 동점으로 설명되는 것이다.

### 4단계: 전체 성능 분석

- 3/5/13/25 크기 벤치마크 구현
- I/O를 배제한 타이머 경계 검증
- 표 출력 구현

완료 기준: 모든 크기에 평균 ms와 N²이 출력되고 반복 횟수가 10 이상이다.

### 5단계: 보너스 구현

- Cross/X 패턴 생성기 구현
- 2D→1D 평탄화 및 `mac_1d` 구현
- 동일 점수 검증과 최적화 전후 성능 표 구현
- 보너스 메뉴와 테스트 작성

완료 기준: 생성 패턴이 정의와 일치하고 2D/1D 결과가 epsilon 내에서 동일하다.

### 6단계: 종합 검증과 실제 결과 수집

- 전체 자동 테스트 실행
- 실제 `data.json` 모드 실행
- 필수 수동 오류 입력 테스트
- 실제 성능값과 실패 케이스 기록
- 외부 라이브러리 import 여부와 Python 3.8 호환성 확인

완료 기준: 평가 체크리스트 각 항목에 테스트 또는 실행 근거가 있다.

### 7단계: README 최종 작성

README는 구현 전에 예상 결과로 채우지 않고, 6단계에서 확인한 실제 동작과 수치로 마지막에 작성한다.

포함할 내용은 다음과 같다.

1. 프로젝트 소개와 MAC 원리
2. 디렉터리 구조
3. Python 버전 및 외부 라이브러리 미사용 명시
4. `python main.py` 실행법과 세 모드 사용법
5. `data.json` 위치와 스키마
6. 핵심 구현 설명: MAC, 키 매칭, 라벨 정규화, epsilon, 오류 격리
7. 자동 테스트 실행법과 실제 테스트 결과
8. 실제 모드 2 총/통과/실패 결과 및 실패 케이스
9. 최소 10줄 이상의 결과 리포트: 동점 FAIL 3건의 원인과 스키마/로직 오류가 아닌 이유
10. 3/5/13/25 실제 성능 표와 O(N²) 분석
11. 보너스 생성기 및 2D/1D 성능 비교 결과
12. 평가항목 답변과 심층 인터뷰 답변

## 11. README 평가항목 답변 구성

README 후반에 `평가항목 해설` 섹션을 두고 체크박스를 복사하는 대신 코드 위치, 실행 결과, 설계 이유를 연결해 답한다.

### 코드 구조 및 설계 답변

- MAC 흐름: 입력 검증 → 같은 위치 곱셈 → 누적 → float 점수 반환 → epsilon 판정
- 키 매칭: 정규식으로 N 추출 → `size_N` 생성 → 필터 조회 → 실제 행렬 크기 재검증
- 정규화 분리 이유: 데이터 표현 차이를 도메인 판정에서 제거하고 새 라벨 확장을 한곳으로 제한
- 측정 경계: 입력/출력/JSON/검증/평탄화를 제외하고 MAC 함수 호출만 측정

### 핵심 원리 답변

- MAC 점수와 유사성: 같은 위치의 활성값이 겹칠수록 내적 누적값 증가
- O(N²): N개 행마다 N개 열을 방문해 정확히 N²번 곱하고 더함
- 부동소수점/epsilon: 2진수로 정확히 표현할 수 없는 소수와 누적 반올림 때문에 근소 차이를 수학적 동점으로 처리
- 모드별 동점 표현: 같은 비교 결과를 사용자 입력 모드에서는 `판정 불가`, 자동 채점 모드에서는 표준 `UNDECIDED`와 FAIL로 표현

### 심층 인터뷰 답변

- 새 라벨 `o`: 표준 라벨 정의 → alias 정규화 → 해당 필터 판정의 일반화 → 출력 → fixture/경계 테스트 순서로 확장
- 1000×1000: Python 객체 기반 2차원 리스트의 메모리, 백만 번의 인터프리터 반복, 캐시 비효율을 분석하고 평탄화/데이터 표현 개선/작업 분할을 우선 검토
- CPU와 NPU: 이 구현의 병목은 반복문 실행과 메모리 접근이며, NPU는 다수 MAC 유닛과 데이터 재사용을 통해 병렬화하지만 메모리 대역폭과 데이터 이동이 새로운 병목이 될 수 있음

최종 README에서는 위 요지를 실제 함수명, 파일 위치, 테스트 결과와 연결하여 평가자가 코드를 찾지 않아도 설계 의도를 확인할 수 있게 작성한다.

## 12. 요구사항 추적표

| 요구사항 | 구현 위치 | 검증 근거 |
|---|---|---|
| 모드 1 입력/재입력 | `modes.py`, `validation.py` | 입력 mock 통합 테스트 |
| 2D MAC | `core.py: mac_2d` | 예시 5/1 단위 테스트 |
| epsilon 동점 | `core.py: compare_scores` | 경계 단위/모드 통합 테스트 |
| JSON 키-필터 매칭 | `loader.py` | 정상/오류 fixture 테스트 |
| meta 버전/형식 경고 | `loader.py` | 정상/누락/불일치 테스트 |
| 라벨 정규화 | `labels.py` | alias/오류 단위 테스트 |
| 케이스 단위 FAIL | `loader.py`, `modes.py` | invalid fixture 연속 실행 테스트 |
| 10회 평균/N² 표 | `benchmark.py`, `presentation.py` | 결과 필드와 출력 테스트 |
| 결과 요약 | `presentation.py` | 합계/실패 목록 테스트 |
| 패턴 생성기 | `generator.py` | 3×3/5×5 배열 테스트 |
| 1D 최적화 | `core.py`, `benchmark.py` | 2D/1D 점수 및 성능 비교 |
| 결과 리포트/인터뷰 답변 | `README.md` | 구현 완료 후 최종 검토 |

## 13. 설계상 주의점

- 실제 `data.json`의 구조와 기준 결과를 확인했으며 이를 구현 기준으로 사용한다. 이후 파일 버전이 바뀌면 `meta` 경고와 스키마 검증으로 변화가 드러나게 한다.
- 내적 점수는 필터의 값 범위나 정규화 방식이 크게 다르면 일반적인 유사도 척도가 아닐 수 있다. 이 과제에서는 같은 크기와 규칙으로 만들어진 두 필터를 비교한다는 전제에서 사용한다.
- `abs(diff) < epsilon`은 요구사항을 그대로 따른다. 정확히 epsilon인 경우는 동점이 아니다.
- 벤치마크는 절대 성능 보장이 아니라 크기 증가 경향을 관찰하는 도구다.
- 1D 방식이 모든 Python 환경에서 반드시 더 빠르다고 단정하지 않는다. 실제 측정값과 변환 비용을 함께 보고한다.

## 14. 실제 데이터 반영 설계 결정

| 결정 | 이유와 목적 |
|---|---|
| `meta`는 경고 수준으로 검증 | 버전 변화는 알리되 계산 가능한 데이터 분석을 불필요하게 막지 않는다. |
| 점수와 판정을 `Optional`로 저장 | 계산 불가 오류와 실제 0점을 구별한다. |
| 총 6개/PASS 3개/FAIL 3개를 기준값으로 사용 | 구현 결과를 독립적으로 검증할 수 있는 명확한 기준을 만든다. |
| 세 동점을 수치 비교 FAIL로 분류 | 정상적인 정책 결과와 프로그램 버그를 구분한다. |
| 크기별 벤치마크 입력을 고정 | 같은 조건에서 N² 증가에 따른 시간 변화를 비교한다. |
| 잘못된 벤치마크 데이터는 SKIP | 생성 데이터 대체로 원본 데이터 오류가 감춰지는 것을 막는다. |
| 실제 실수 데이터를 테스트에 사용 | epsilon이 이론상 장식이 아니라 실제 판정에 필요함을 검증한다. |
| README에 실제 세 FAIL을 분석 | 평가자가 결과와 정책의 관계를 바로 이해하고 학생도 이유를 설명할 수 있게 한다. |
